Project Overview:

Use the arrow keys to move Pacman! An executable JAR is included with "target", and project is configured as a Maven project. 

The project requires a custom-made local JAR (cs0150-1.0.0.jar), which is included in the root directory. Move this file into your local Maven repository before building (and update the POM file accordingly) if looking to make changes